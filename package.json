{
  "name": "starter",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "env:dev": "cp -f ./env/.dev ./env/index.js",
    "env:uat": "cp -f ./env/.prod ./env/index.js",
    "env:prod": "cp -f ./env/.prod ./env/index.js",
    "start": "yarn env:dev && react-native start --reset-cache",
    "android:debug": "yarn env:dev && react-native run-android",
    "android:release": "yarn env:dev && react-native run-android --variant=release",
    "android:build": "yarn env:dev && cd android && ./gradlew bundleRelease && cd ..",
    "ios:debug": "yarn env:dev && react-native run-ios",
    "ipad:debug": "yarn env:dev && react-native run-ios --simulator='iPad (9th generation)'",
    "clean": "rm yarn.lock && rm -rf node_modules && yarn",
    "clean:android": "cd android && ./gradlew clean && cd ..",
    "clean:ios": "cd ios && xcodebuild clean && cd ..",
    "pod:update": "cd ios && pod install --repo-update && cd ..",
    "pod:install": "cd ios && pod install && cd ..",
    "clean:all": "yarn clean && yarn clean:ios && yarn clean:android"
  },
  "dependencies": {
    "@react-native-async-storage/async-storage": "^1.17.4",
    "@react-native-khan/helpers": "^0.0.6",
    "@react-navigation/native": "^6.0.10",
    "axios": "^0.27.2",
    "react": "17.0.2",
    "react-native": "0.68.2",
    "react-native-device-info": "^8.7.1",
    "react-native-snackbar": "^2.4.0"
  },
  "devDependencies": {
    "@babel/core": "^7.12.9",
    "@babel/runtime": "^7.12.5",
    "@react-native-community/eslint-config": "^2.0.0",
    "babel-jest": "^26.6.3",
    "eslint": "^7.32.0",
    "jest": "^26.6.3",
    "metro-react-native-babel-preset": "^0.67.0",
    "react-test-renderer": "17.0.2"
  },
  "jest": {
    "preset": "react-native"
  }
}
